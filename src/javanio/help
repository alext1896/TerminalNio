ls: Hace un listado de los directorios y archivos de la ruta en la que se encuentra.

cd <nombre directorio>: Este comando permite accede a un directorio de manera que al escribir cd y el nombre de un directorio accederá a él.

cd : Muestra el directorio actual.

cd .. : Accede al directorio padre.

cd <nombre_directorio>: Accede a un directorio dentro del directorio actual.

cd <C:\Nombre_Directorio>: Accede a una ruta absoluta del sistema.

dir: Lista los archivos contenidos en el directorio actual.

mkdir <nombre_directorio>: Crea un directorio en la ruta actual.

Info <nombre_archivo_o_directorio> este comando imprimirá los datos básicos de un archivo o directorio: su nombre, nombre de la carpeta padre, el tamaño del archivo o la carpeta.Acceso a datos Desarrollo web Entorno servidor Despliegue de aplicaciones web.

cat <nombre_fichero> : muestra el contenido de un fichero a través del terminal.

top <numero_lineas> <nombre_fichero>: muestras las primeras <numero_lineas> de un fichero. De manera que top 20 hola.txt muestra las primeras 20 líneas del fichero hola.txt

mkfile <nombre_fichero> <texto>: Crea un nuevo fichero llamado <nombre fichero> y escribe en el <texto>.

write <nombre_fichero> <texto>: Añade <texto> al final de <nombre_fichero> que ya existe.

Delete <nombre_archivo_o_directorio>: Borra un archivo o una carpeta, en caso de ser una carpeta borra todos los archivos que la contienen.

dir: listas los archivos u directorios de la ruta actual.

dir <nombre_directorio>: listas los archivos y directorios de <nombre_directorio>-

dir <C:\Nombre_Directorio>: listas los archivos y directorios de una ruta absoluta.

cp <fichero_original> <fichero_copia>: Realiza una copia del fichero denominado cp <fichero_original> al <fichero_copia>:. El <fichero_original> y/o el <fichero_copia>: se podrán especificar con el nombre de un archivo en el directorio local o una ruta absoluta al fichero. 
En caso de que el nombre del <fichero_copia> ya exista se dará la opción de sobreescribirlo.